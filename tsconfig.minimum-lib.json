{
	// Compiles the public types against the lowest `lib` we support to ensure no type references a global that consumers on older libs don't have.
	"extends": "./tsconfig.json",
	"compilerOptions": {
		"target": "ES2020",
		"lib": ["ES2020"]
	},
	"include": [
		"index.d.ts"
	]
}
